# Examples

Use this directory for small C++20 examples that demonstrate one template
mechanism at a time.

Prefer names that expose the lesson, such as `dependent_name.cpp`,
`partial_specialization.cpp`, `sfinae_overload.cpp`, or `fold_expression.cpp`.
